<h1>All Users</h1> 
<%= button_to "Create new user", new_user_path, :method => :get %>

<ul>

    <% @users.each do |user| %>
        <li> <%= link_to user.name, user_path(user.id) %></li> 
    <% end %>

</ul>
